export async function load() {
/*     let response = await fetch("/data/data_index.csv");
    let text = await response.text();
    let lines = text.split("\n");
    return lines.map((line) => line.split(",")[2]);
 */}